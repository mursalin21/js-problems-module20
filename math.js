//Absolute value
const myNumber = -5;
const output = Math.abs(myNumber);


//ceil, floor, round
const myNumber = 6.6398;
const output = Math.ceil(myNumber);
const output = Math.floor(myNumber);
const output = Math.round(myNumber);

//random value generation
//Use multiplication to get specific ranges of random value
const output = Math.round(Math.random() * 6);
console.log(output);

